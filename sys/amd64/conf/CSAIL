cpu		HAMMER
ident		CSAIL

makeoptions	DEBUG="-g"		# Build kernel with gdb(1) debug symbols
makeoptions	WITH_CTF=1		# Build kernel with dtrace(8) symbols

options 	SCHED_ULE		# ULE scheduler
options 	PREEMPTION		# Enable kernel thread preemption
options		NUMA
options         EFIRT                   # EFI Runtime Services support
options 	INET			# InterNETworking
options 	INET6			# IPv6 communications protocols
options 	SCTP_SUPPORT		# Provision for run-time loadable SCTP
options		KERN_TLS		# TLS transmit & receive offload
options         TCP_OFFLOAD             # TCP offload
options         TCP_BLACKBOX            # Enhanced TCP event logging
options         TCP_HHOOK               # hhook(9) framework for TCP
options         TCP_RFC7413             # TCP Fast Open
options 	FFS			# Berkeley Fast Filesystem
options 	SOFTUPDATES		# Enable FFS soft updates support
options 	NFSCL			# New Network Filesystem Client
options 	NFSD			# New Network Filesystem Server
options 	NFSLOCKD		# Network Lock Manager
options		KGSSAPI			# Kernel GSSAPI (needs userland supprt)
options 	MSDOSFS			# MSDOS Filesystem
options 	CD9660			# ISO 9660 Filesystem
options		TMPFS			# Efficient memory filesystem
options 	GEOM_PART_GPT		# GUID Partition Tables.
options 	GEOM_LABEL		# Provides labelization
options		GEOM_MULTIPATH		# Allows for multipathing
options		GEOM_MIRROR		# Simple mirroring (for swap)
options 	KTRACE			# ktrace(1) support
options 	STACK			# stack(9) support
options 	SYSVSHM			# SYSV-style shared memory
options 	SYSVMSG			# SYSV-style message queues
options 	SYSVSEM			# SYSV-style semaphores
options 	_KPOSIX_PRIORITY_SCHEDULING # POSIX P1003_1B real-time extensions
options 	PRINTF_BUFR_SIZE=128	# Prevent printf output being interspersed.
options 	KBD_INSTALL_CDEV	# install a CDEV entry in /dev
options 	HWPMC_HOOKS		# Necessary kernel hooks for hwpmc(4)
options 	AUDIT			# Security event auditing
options 	MAC			# TrustedBSD MAC Framework
options 	KDTRACE_FRAME		# Ensure frames are compiled in
options 	KDTRACE_HOOKS		# Kernel DTrace hooks
options		DDB_CTF			# Load DTrace symbols
options 	INCLUDE_CONFIG_FILE     # Include this file in kernel
options 	KDB			# Kernel debugger related code
options 	KDB_TRACE		# Print a stack trace for a panic
options		COMPAT_FREEBSD32	# Can run 32-bit binaries
options 	COMPAT_FREEBSD10	# Can run FreeBSD 10.x binaries
options		COMPAT_FREEBSD11	# Can run FreeBSD 11.x binaries
options		COMPAT_FREEBSD12	# Can run FreeBSD 12.x binaries
options 	DDB			# Build in the debugger
options 	KDB_UNATTENDED		# Don't go to debugger on panic
options 	BREAK_TO_DEBUGGER	# Go to debugger on serial console brk
#options		PROCDESC
options         CAPABILITY_MODE         # Capsicum capability mode
options         CAPABILITIES            # Capsicum capabilities
options		PPS_SYNC		# Allow ntpd to use kernel PPS
options		VIMAGE			# Virtual networking stacks for jails
options 	PCI_IOV
options		PCI_HP			# Support PCIe hot-plug (for NVMe)

# Kernel dump features.
options        GZIO                    # gzip-compressed kernel and user dumps
options        ZSTDIO                  # zstd-compressed kernel and user dumps

# Make an SMP-capable kernel by default
options 	SMP			# Symmetric MultiProcessor Kernel

# CPU frequency control
device		cpufreq
options 	EARLY_AP_STARTUP	# Start auxiliary CPUs early in boot
# EARLY_AP_STARTUP is apparently mandatory for systems that have
# certain cpufreq(4) implementations, otherwise hwpstate_intel hangs
# trying to configure non-boot processors that haven't been initialized
# yet.

# Bus support.
device		acpi
options         IOMMU
device		pci

# Floppy drives
device		fdc

# ATA controllers
device		ahci		# AHCI-compatible SATA controllers
device		ata		# Legacy ATA/SATA controllers
device		mvs		# Marvell 88SX50XX/88SX60XX/88SX70XX/SoC SATA
device		siis		# SiliconImage SiI3124/SiI3132/SiI3531 SATA

# SCSI Controllers
device		mps		# LSI-Logic MPT-Fusion 2
device		mpr		# Avago MPT SAS-3

# ATA/SCSI peripherals
device		scbus		# SCSI bus (required for ATA/SCSI)
device		da		# Direct Access (disks)
device		cd		# CD
device		pass		# Passthrough device (direct ATA/SCSI access)
device		ses		# SCSI Environmental Services (and SAF-TE)
device		ctl		# CAM Target Layer (iSCSI targets)
options 	CAM_IOSCHED_DYNAMIC	# Get more data about I/O performance

# RAID controllers
device		mfi		# LSI MegaRAID SAS
device		mfip		# LSI MegaRAID SAS passthrough
device		mrsas		# newer LSI MegaRAID SAS

# Non-Volatile Memory Express (NVMe)
device		nvme
device		nvd

# atkbdc0 controls both the keyboard and the PS/2 mouse
device		atkbdc		# AT keyboard controller
device		atkbd		# AT keyboard
device		psm		# PS/2 mouse

device		kbdmux		# keyboard multiplexer

device		vga		# VGA video card driver

device		splash		# Splash screen and screen saver support

# vt is the modern (UEFI-compatible) console driver
# Eventually we should standardize on vt everywhere but I haven't
# tried it anywhere else.
device		vt
device		vt_vga
device		vt_efifb

# Serial (COM) ports
device		uart		# Generic UART driver

# PCI Ethernet NICs.
device		iflib
device		em		# Intel PRO/1000 Gigabit Ethernet Family
device 		ix
device		cxgbe		# Chelsio T4xx 10GbE and T5xx 40GbE families

device         iavf                    # Intel Adaptive Virtual Function
device         ice                     # Intel 800 Series Physical Function
device         axp                     # AMD EPYC integrated NIC


# This driver insta-panics on three servers with on-motherboard X722
# 1G interfaces, so load it explicitly on the machines that have a
# card from this family that works (and continue not to buy any more
# of them).
#device		ixl		# Intel XL710 40GbE family

# PCI Ethernet NICs that use the common MII bus controller code.
# NOTE: Be sure to keep the 'device miibus' line in order to use these NICs!
device		miibus		# MII bus support
device		bce		# Broadcom BCM5706/BCM5708 Gigabit Ethernet
device		bge		# Broadcom BCM570xx Gigabit Ethernet

# Pseudo devices.
device		loop		# Network loopback
device          rdrand_rng              # Intel Bull Mountain RNG
device		ether		# Ethernet support
device		vlan		# 802.1Q VLAN support
device		pty		# BSD-style compatibility pseudo ttys
device		md		# Memory "disks"
device		firmware	# firmware assist module

device		bpf		# Berkeley packet filter

# Virtual machine support
device		virtio
device		virtio_balloon
device		virtio_blk
device		virtio_pci
device		virtio_scsi
device		vtnet

# USB support
options 	USB_DEBUG	# enable debug msgs
device		uhci		# UHCI PCI->USB interface
device		ohci		# OHCI PCI->USB interface
device		ehci		# EHCI PCI->USB interface (USB 2.0)
device		xhci		# XHCI PCI->USB interface (USB 3.0)
device		usb		# USB Bus (required)
device		uhid		# "Human Interface Devices"
device		ukbd		# Keyboard
device		umass		# Disks/Mass storage - Requires scbus and da
device		ums		# Mouse

# HID support
device		hid		# Generic HID support

# System management options
device		ipmi		# Intelligent Platform Management Interface
device		smbios		# System Management BIOS
device		tpm		# Trusted Platform Module
device		ichwd		# Intel ICH watchdog timer
device		amdsbwd		# AMD SB7xx watchdog timer
device		coretemp	# On-die temperature sensor, Intel
device		amdtemp		# On-die temperature sensor, AMD
device		cpuctl		# CPU control pseudo-device
device		hwpmc		# Hardware performance-monitoring counters
device		smbus		# System management bus
device		ichsmb		# Intel ICHx SMBus controller
device		amdsmb		# AMD 8111 SMBus controller
device		smb		# User access to SMBus devices

# In-kernel cryptography
device		crypto		# Software/hardware crypto subsystem
device		aesni		# Intel AES instructions
# Do not enable: there is no performance advantage when it's just software
# crypto, and the implementation in 12.x is buggy.
#device		cryptodev


# Network options
options		IPFIREWALL	# Kernel firewall, traditional FreeBSD flavor
options		IPFIREWALL_DEFAULT_TO_ACCEPT
options		ACCEPT_FILTER_DATA
options		ACCEPT_FILTER_DNS
options		ACCEPT_FILTER_HTTP
#device		netmap

# Random crap that would get auto-loaded by devd(8) anyway
device		ioat
